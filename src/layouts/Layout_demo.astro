---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>💬</text></svg>" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<link rel="stylesheet" href="/src/themes/default.css" />
		<style>
			.controlBar {
				position: absolute; 
				top: 5px; 
				left: 5px; 
				z-index:1; 
				width: 90%; 
				margin: 0; 
				margin-left: 50%; 
				transform: translateX(-50%); 
				border: solid 1px; 
				border-radius: 7px; 
				padding: 4px; 
				background-color: rgba(53, 53, 225, 0.7); 
				color: #fff; font-family: Arial, Helvetica, sans-serif; 
				font-size: 16px;
			}
		</style>
	<!-- INCLUDE CONFIG -->
		<script type="text/javascript" src="/src/scripts/config.js"></script>
	<!-- HANDLE CONFIG & THEME -->
		<script type="text/javascript">
			if (config.theme != "") {
				document.head.getElementsByTagName("link")[1].href = "/src/themes/"+config.theme+".css"
				console.log("theme applied from config", config.theme)
			}
			let baseUrl = window.location.href.split('demo')[0]
			config.overlayBaseURL = baseUrl
			window.CONFIG = config //Object.freeze(config);
		</script>
	<!-- ComfyJS MOCK -->
		<script src="/src/scripts/demo/demo.mjs" type="module"></script>
	<!-- 3rd PARTY -->	
		<script type="text/javascript" src="/src/scripts/demo/faker.min.js"></script>	
<!-- 3rd PARTY -->	
	<script type="text/javascript" src="/src/scripts/comfy.min.js"></script>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background: #13151a;
		background-size: 224px;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
</style>

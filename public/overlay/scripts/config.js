// CONFIG pour handlechat.js (cf https://github.com/BenDMyers/showmy.chat)
const config = {
    "clearMessageAfter":"",           // duration (sec)
    "DEMO":false,                    // DEPRECATED   
    "disableAnimatedEmotes":false,  // enable or not animated emotes ( [true|false] )
    "hideMessagesFrom":"",         // user banned from overlay (user separated with a space char)
    "showCommands":false,         // show commands or not in the overlay ( [true|false] )
    "showLatestMessages":30,     // delete from DOM after (number of messages)
    "theme":"cards"             // default theme when none given in url
};